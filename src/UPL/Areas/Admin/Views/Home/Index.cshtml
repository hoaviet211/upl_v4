@{
    ViewData["Title"] = "Bảng điều khiển";
}

<section class="page-heading">
    <div class="d-flex flex-column flex-lg-row align-items-lg-center justify-content-between gap-3 mb-4">
        <div>
            <h1 class="page-title">Bảng điều khiển</h1>
            <p class="text-muted mb-0">Chọn module làm việc ở khu vực quản trị.</p>
        </div>
    </div>
</section>

<div class="row g-4">
    <div class="col-12 col-lg-6">
        <div class="card admin-card h-100">
            <div class="card-body d-flex flex-column gap-3">
                <div>
                    <h5 class="fw-semibold mb-1">Ban điều hành</h5>
                    <p class="text-muted mb-0">Quản lý tài khoản nhân sự, phân quyền và trạng thái hoạt động.</p>
                </div>
                <div class="d-flex flex-column flex-md-row gap-2">
                    <a asp-area="Admin" asp-controller="Employees" asp-action="Index" class="btn btn-primary">Danh sách Ban điều hành</a>
                    <a asp-area="Admin" asp-controller="Employees" asp-action="Create" class="btn btn-outline-secondary">Thêm Ban điều hành</a>
                </div>
            </div>
        </div>
    </div>
</div>

